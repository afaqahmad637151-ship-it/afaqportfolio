// frontend/public/js/components/about.js

/**
 * Initializes the About section specific functionalities.
 */
export function initializeAbout() {
    // No dynamic content loading for now, as it's static text.
    // All animations for About section are handled by animations.js based on scroll.
}